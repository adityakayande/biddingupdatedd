import { topList } from "../../utils/data";
import { Caption, Container, Heading } from "../../router";

export const TopCollection = () => {
  // Temporarily hidden to remove mock data
  return null;
};
